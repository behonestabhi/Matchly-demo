# Core runtime dependencies (light install — keeps the image small and lets
# tests run without ML model downloads). Optional heavy deps are imported
# lazily and are intentionally NOT listed here:
#   - sentence-transformers  (embedding upgrade; hashing fallback otherwise)
#   - spacy + en_core_web_sm  (name NER; regex fallback otherwise)
#   - psycopg[binary]         (pgvector persistence; in-memory otherwise)
#   - PyMuPDF / pymupdf       (PDF extraction; pdfplumber is the listed path)

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
numpy>=1.26
scikit-learn>=1.4
python-docx>=1.1
pdfplumber>=0.11
httpx>=0.27

# Test-only (kept here so `pip install -r requirements.txt` enables `pytest`).
pytest>=8.0
